# AGENTS.md

本文件适用于整个仓库。处理任务时使用中文沟通，从第一性原理分析问题，不发送非必要的过程说明。

## 项目概览

imap2api 将多个 IMAP 邮箱同步到本地加密 SQLite 缓存，并通过 Fastify API 和 React 管理端提供访问能力。

- `apps/server`：Fastify、ImapFlow、SQLite、SSE 和静态资源托管。
- `apps/web`：React 19 + Vite 管理端。
- `packages/shared`：前后端共享类型和 API 契约。
- `apps/server/src/account-supervisor.ts`：账号与文件夹会话生命周期、IDLE、轮询、重连和同步调度。
- `apps/server/src/mail-sync.ts`：邮件窗口校准、MIME 正文解析和缓存变更事件。
- `apps/server/src/imap.ts`：IMAP 服务门面及已读写操作。

## 开发约定

- 使用 TypeScript 和 ESM，遵循现有模块边界与命名风格。
- 优先修改最小必要范围，不进行无关重构、依赖升级或格式化。
- 共享响应结构或字段发生变化时，先更新 `packages/shared`，再同步服务端、管理端和测试。
- 手工编辑文件使用补丁方式，保持 ASCII；只有中文文案和已有中文文件可以使用 Unicode。
- 不提交 `dist`、本地数据库、日志、Token、邮箱凭据或其他运行时数据。
- 不撤销或覆盖工作区中来源不明的现有改动。

## 后端关键约束

- IDLE 邮箱必须保留 ImapFlow auto-IDLE；只有不支持 IDLE 的会话可以禁用 auto-IDLE，并由本服务按设置执行 STATUS 轮询。
- 每个文件夹同一时间只能运行一个同步任务；并发通知通过 dirty 标记合并，不能重叠执行 IMAP 命令。
- 不使用 `SEARCH ALL` 获取缓存窗口；根据已选邮箱的 `exists` 读取最后 N 条序列范围，并从 FETCH 结果获取 UID。
- 完成元数据 FETCH 后才能单独读取未知邮件正文，避免嵌套 IMAP 命令死锁。
- IMAP 写操作成功后才能更新 SQLite；批量操作需要保留其他文件夹的成功结果。
- SQLite 结构变化必须使用向后兼容迁移，不得破坏现有加密数据。
- 不改变 Token 派生、AES-256-GCM 加密格式或现有数据库密钥校验方式，除非任务明确要求迁移方案。
- SSE 只能发送缓存变更元数据，不能发送主题、地址、正文或凭据；关闭服务时必须先结束 SSE 长连接。
- API 错误保持 `{ error: { code, message, details? } }`，不得向客户端返回堆栈、SQLite 错误、凭据或内部路径。
- 附件只保存文件名，不下载、不缓存、不提供下载接口。

## 前端关键约束

- Token 只保存在 `sessionStorage`，不得写入持久化存储、URL 或日志。
- 使用现有 CSS Modules、Radix primitives、Lucide 和 Motion，不引入新的 UI 框架。
- 邮件 HTML 必须继续在受限 iframe 中展示，不能放宽服务端净化或 iframe 沙箱。
- SSE 必须通过带 Bearer Header 的 `fetch()` 订阅，并在退出或组件卸载时取消连接。

### 美学引擎

- 产品主角是“持续流动且可控的邮件连接”，不是品牌宣传或装饰图形。
- 核心操作仪式是快速确认连接状态、浏览邮件并完成已读处理；页面层级必须优先服务扫描、比较和重复操作。
- 期望余韵是“系统正在可靠运转，我随时知道发生了什么”。
- 中心张力来自远端 IMAP 的异步和不确定性，与本地界面的即时、稳定、可预测之间的对照。
- 美学命题是用安静空间、清晰数据层级和有因果关系的微动效，让不可见的同步始终可感知但不打扰工作。
- 标志性交互是“同步轨迹”：只在真实同步期间显示，结束后收束到状态和最近同步时间，不伪造百分比或无限循环进度。
- 资产策略固定为 `deliberately none`：这是高频运维工具，不生成装饰图片、插画或氛围素材；使用 Lucide 功能图标和真实邮件数据承载内容。未来只有核心任务确实需要媒体时才重新评估。

### 视觉语法

- 借鉴 Apple HIG 的克制、清晰和物理反馈，但不得复制 Apple 品牌资产、页面结构或专有视觉元素。
- 保持紧凑、简约、工作导向的管理工具气质；不制作营销 Hero、功能宣传区或编辑式大标题构图。
- 使用语义化 CSS 变量维护背景、表面、文字、边框、强调色和状态色；新增颜色前先确认不能由现有令牌表达。
- 主背景使用近白中性色，正文近黑，次要信息使用两级灰；系统蓝只用于主操作、选择、焦点和同步轨迹。
- 成功、警告、错误使用绿、琥珀和红，并始终搭配图标或文字，不能只依赖颜色传达状态。
- 字体使用系统栈，不加载外部字体；字号保持 13、14、16、20、28px 固定层级，字距为 `0`，不按视口缩放字体。
- 使用 8px 空间网格。桌面控件约 36px 高，触屏交互目标至少 44px；固定格式控件必须有稳定尺寸，状态变化不能引发布局跳动。
- 普通控件圆角 6px，面板或卡片不超过 8px；胶囊形只用于状态标签和分段控制。
- 阴影只用于菜单、对话框和确实需要脱离页面层级的浮层；半透明只用于顶栏或侧栏，并提供无模糊纯色回退。
- 禁止渐变背景、装饰光斑、漂浮色块、无意义玻璃拟态、嵌套卡片和用多个卡片切碎页面结构。
- 卡片只用于重复条目、对话框和真正需要边界的工具区域；页面章节本身保持无框或全宽布局。
- 图标按钮优先使用 Lucide 熟悉符号，并为不明确的图标提供 Tooltip；有标准图标时不要用带文字的圆角矩形代替。

### 构图与响应式

- 桌面端维持固定窄侧栏、紧凑顶栏和主工作区；邮件页使用列表与详情双栏，优先保证列表扫描和正文阅读的平衡。
- 移动端使用单栏逐层进入，导航、邮件列表和邮件详情分别占据完整视图，并保留明确返回路径。
- 登录页只使用一个紧凑认证面板，不添加营销文案、插画或功能说明。
- 账号页使用紧凑列表展示邮箱、服务商、状态、同步模式、最近同步和错误摘要，不改成卡片墙。
- 设置页只承载系统级参数，避免无关说明、装饰区域和重复容器。
- 长邮箱地址、主题、中英文正文、附件名和错误信息必须在 390px、1024px 和 1440px 宽度下正确换行或截断，不能溢出、遮挡或撑开固定控件。
- 稳定使用 `grid`、`minmax()`、`aspect-ratio` 和明确的最小/最大尺寸；动态内容、悬停和加载状态不能改变工具栏、列表行或按钮尺寸。
- 不隐藏任务所需的比较信息来追求空旷；紧凑不等于拥挤，留白必须服务分组、节奏和注意力。

### 动效与反馈

- Motion for React 与 CSS 使用统一动效令牌，主缓动采用快速启动、柔和停止的 ease-out 曲线。
- 直接反馈控制在 100–160ms，列表或面板连续性控制在 180–240ms，复杂容器过渡不超过 320ms。
- 动效只修改 `transform`、`opacity`、颜色、边框或阴影，不动画宽高和定位属性。
- 按钮按压最多缩放至 0.98；悬停只改变 3–6% 的背景明度，不平移文字、不弹跳。
- 页面切换只保留 4–8px 的轻微空间连续性；列表筛选、邮件选择和详情切换必须能看出来源与去向。
- 除真实同步状态外，不使用自主循环动画；同一视口最多保留一个高显著动效，邮件正文阅读区域保持静止。
- 所有动效必须可中断，不阻塞输入；重复点击、请求失败、后台切换和取消后必须落到真实最终状态。
- `prefers-reduced-motion` 下取消位移、旋转、弹性和同步轨迹运动，改用即时状态切换、静态图标和文字，功能信息不得丢失。
- 保存、同步、标记和错误反馈应出现在操作发生处；高影响操作使用确认对话框，不能依赖全局通知代替必要的局部状态。

### 交互、状态与可访问性

- 功能必须覆盖 loading、empty、success、warning、error、disabled、partial success 和 reconnecting 等真实状态，禁止用静态演示状态冒充运行结果。
- 不在页面中加入描述视觉风格、键盘快捷键或“如何使用本界面”的自我说明文字；界面应通过层级、标签和反馈自然说明自己。
- 保持可见焦点、合理的 Tab 顺序、对话框焦点圈定和关闭后的焦点归还；所有图标按钮必须有可访问名称。
- 文本和交互状态达到 WCAG AA 对比度；验证 200% 缩放、键盘操作、屏幕阅读顺序和颜色冗余表达。
- 不允许文字与图标、按钮、列表行、浮层或后续内容发生非预期重叠；最长单词仍无法容纳时，应调整布局或安全断行。
- 修改视觉或交互前先声明 Create、Redesign、Refine 或 Review 模式及变更边界，保护真实数据、功能、状态语义和响应式路径。
- 交付前检查整页轮廓、视觉重心、阅读路径、信息负荷、状态真实性和跨页面一致性；任何醒目视觉动作都必须能追溯到主角、仪式或核心任务。

## 常用命令

要求 Node.js 22 或更高版本。CI 使用 Node.js 24。

```bash
npm ci
npm run typecheck
npm test
npm run build
npm audit --omit=dev --audit-level=high
docker build -t imap2api:local .
```

聚焦验证可以使用：

```bash
npm run typecheck -w @imap2api/server
npm run test -w @imap2api/server -- src/imap.test.ts
npm run test -w @imap2api/web
```

## 验证要求

- 修改共享类型、数据库、认证、同步状态机或 API 时，必须运行完整类型检查和测试。
- 修改构建、依赖、Docker 或运行配置时，必须运行生产构建和对应的 Docker/审计检查。
- 修复并发或生命周期问题时，测试必须覆盖失败、取消、重连和关闭路径，不能只覆盖成功路径。
- 前端布局或交互变化需要验证桌面与移动视口、键盘焦点、长文本和 reduced-motion。
- 无法运行的检查必须明确记录原因，不得将外部服务失败描述为代码验证通过。

## Git 约定

- 提交前检查 `git status`、`git diff --check` 和暂存区内容。
- 提交信息使用简洁的 Conventional Commits 风格，例如 `fix: serialize mailbox synchronization`。
- 除非用户明确要求，不自动提交、推送、创建标签或修改发布产物。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户改动。
